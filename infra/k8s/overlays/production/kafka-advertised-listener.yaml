apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: kafka
spec:
  template:
    spec:
      containers:
      - name: kafka
        env:
        - name: KAFKA_ADVERTISED_LISTENERS
          value: SSL://kafka.chronoverse.svc.cluster.local:9094
