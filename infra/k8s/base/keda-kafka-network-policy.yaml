apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: kafka-from-keda
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: kafka
  policyTypes:
  - Ingress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          chronoverse.io/keda-kafka-access: "true"
    ports:
    - protocol: TCP
      port: 9094
